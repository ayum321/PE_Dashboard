# ── Performance Engineering Dashboard — Backend Stack ─────────
# FastAPI + Uvicorn + Pydantic v2 (decoupled from Streamlit)

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Templating + form/upload support
jinja2>=3.1.3
python-multipart>=0.0.9

# Document parsing (preserved from app_v2.py)
pypdf>=4.2.0
python-docx>=1.1.0
lxml>=5.2.0
beautifulsoup4>=4.12.3

# Batch / Ctrl-M analytics (Phase 3)
pandas>=2.2.0
numpy>=1.26.0
openpyxl>=3.1.2
xlrd>=2.0.1

# AI Insights — Google Gemini (new SDK primary + legacy fallback)
google-genai>=1.0.0
google-generativeai>=0.8.0

# Gemini Vision: image extraction from PDF/DOCX
PyMuPDF>=1.23.0
Pillow>=10.0.0

# Azure Monitor — personal identity via az login / browser login
azure-identity>=1.16.0
azure-monitor-query>=1.3.0,<2.0.0
azure-mgmt-compute>=30.0.0
azure-mgmt-resource>=23.0.0
azure-mgmt-subscription>=3.0.0
azure-mgmt-resourcegraph>=8.0.0
