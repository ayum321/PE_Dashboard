# Legacy FastAPI dashboard

This folder contains only the retired FastAPI browser UI assets. Run it locally with `start.bat`.

It is retained for parity comparison and local review. It is not the Blue Yonder Portal deployment target.

The shared processing API remains in `..\app`; it is intentionally not copied here, so SLA, resource, findings, and export logic has one implementation.
