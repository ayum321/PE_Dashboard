<section class="panel"><h2>Methodology and lineage</h2><p><strong>Job SLA:</strong> {{ report.methodology.job_sla_def }}<br><strong>Window SLA:</strong> {{ report.methodology.window_sla_def }}<br><strong>Buffer formula:</strong> {{ report.methodology.buffer_formula }}</p><p><strong>Status bands:</strong> {% for status, definition in report.methodology.status_bands.items() %}{{ status }} {{ definition }}{% if not loop.last %} · {% endif %}{% endfor %}</p><p class="footnote">Source badges: {{ report.methodology.source_badges|join(', ') or 'No sources loaded' }}.</p></section>
